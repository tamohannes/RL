[build-system]
requires = ["setuptools>=77"]
build-backend = "setuptools.build_meta"

[project]
name = "sciprobe-nemo-gym-extensions"
version = "0.0.1"
requires-python = ">=3.11"

[tool.setuptools]
py-modules = ["sciprobe_capability", "sciprobe_capability_store"]
